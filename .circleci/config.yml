version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: furtherreach/label
    steps:
      - run: echo "Hi I'm on Runners!"
      - run: touch /tmp/test
